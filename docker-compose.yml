version: "3.9"

services:
  db:
    image: postgres:14
    environment:
      POSTGRES_DB: blogproject_xuzg
      POSTGRES_USER: blogproject_xuzg_user
      POSTGRES_PASSWORD: NCKWBtQDk9eSDS2EDZC4KZ0CyMv0QZXg
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bloguser -d blogdb"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    command: bash -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      POSTGRES_DB: blogproject_xuzg
      POSTGRES_USER: blogproject_xuzg_user
      POSTGRES_PASSWORD: NCKWBtQDk9eSDS2EDZC4KZ0CyMv0QZXg
      POSTGRES_HOST: dpg-d1ijqimr433s73ap17ag-a
      POSTGRES_PORT: 5432
      SECRET_KEY: django-insecure-+3mt(!3mjsbzqu$w4l^5iond10_=usuw2sb8ahe@mb828q62=h  
      DEBUG: "True"
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data: